// Visual admin polish is now implemented in React components and CSS.
// This module intentionally performs no DOM injection so order counts, filters,
// and active sections always come from the canonical React data flow.
export {};
