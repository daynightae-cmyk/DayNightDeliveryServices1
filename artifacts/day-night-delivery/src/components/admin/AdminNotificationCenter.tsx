import { useEffect, useMemo, useState } from "react";
import { Bell, CheckCheck, Trash2, Volume2, VolumeX, X } from "lucide-react";
import { clearAdminNotifications, markAdminNotificationsRead, readAdminAudioSettings, readAdminNotifications, writeAdminAudioSettings, type AdminNotification } from "../../lib/adminAudio";

function useNotifications() { const [items, setItems] = useState<AdminNotification[]>(() => readAdminNotifications()); useEffect(() => { const sync = () => setItems(readAdminNotifications()); window.addEventListener("dn-admin-notifications-change", sync); window.addEventListener("storage", sync); return () => { window.removeEventListener("dn-admin-notifications-change", sync); window.removeEventListener("storage", sync); }; }, []); return [items, setItems] as const; }
export function AdminNotificationBell({ isArabic, onOpen }: { isArabic: boolean; onOpen: () => void }) { const [items] = useNotifications(); const [muted, setMuted] = useState(() => readAdminAudioSettings().muted); const unread = items.filter((item) => !item.read).length; const toggleMute = () => { const next = { ...readAdminAudioSettings(), muted: !muted }; writeAdminAudioSettings(next); setMuted(next.muted); }; return <div className="dn-admin-notification-bell"><button type="button" onClick={onOpen} aria-label={isArabic ? "الإشعارات" : "Notifications"}><Bell className="h-4 w-4" />{unread > 0 && <span>{unread > 9 ? "9+" : unread}</span>}</button><button type="button" onClick={toggleMute} aria-label={muted ? (isArabic ? "تشغيل الصوت" : "Unmute") : (isArabic ? "كتم" : "Mute")}>{muted ? <VolumeX className="h-4 w-4" /> : <Volume2 className="h-4 w-4" />}</button></div>; }
export default function AdminNotificationCenter({ isArabic, open, onClose }: { isArabic: boolean; open: boolean; onClose: () => void }) { const [items, setItems] = useNotifications(); const text = (ar: string, en: string) => isArabic ? ar : en; const unreadIds = useMemo(() => items.filter((item) => !item.read).map((item) => item.id), [items]); const markRead = () => { markAdminNotificationsRead(unreadIds); setItems(readAdminNotifications()); }; const clear = () => { clearAdminNotifications(); setItems([]); }; if (!open) return null; return <aside className="dn-admin-notification-drawer" dir={isArabic ? "rtl" : "ltr"}><header><div><span>DAY NIGHT</span><h2>{text("الإشعارات", "Notifications")}</h2></div><button type="button" onClick={onClose}><X className="h-4 w-4" /></button></header><div className="dn-admin-notification-actions"><button type="button" onClick={markRead}><CheckCheck />{text("تحديد كمقروء", "Mark read")}</button><button type="button" onClick={clear}><Trash2 />{text("مسح الكل", "Clear all")}</button></div><div className="dn-admin-notification-list">{items.length === 0 ? <p>{text("لا توجد إشعارات حالياً", "No notifications right now")}</p> : items.map((item) => <article key={item.id} className={`dn-admin-notification-row is-${item.priority || "normal"} ${item.read ? "is-read" : ""}`}><div><span>{item.type}</span>{item.priority === "high" && <b>{text("أولوية عالية", "High priority")}</b>}</div><strong>{isArabic ? item.titleAr : item.titleEn}</strong><p>{isArabic ? item.bodyAr : item.bodyEn}</p><small>{text("منذ قليل", "Just now")}</small></article>)}</div></aside>; }
