import { useMemo, useState } from "react";
import { FileText, Filter, Search, Sparkles } from "lucide-react";
import AdminLiveOperationsMap from "./AdminLiveOperationsMap";
import AdminPdfExportButton from "./AdminPdfExportButton";
import AdminFinanceOperationsCenter from "./AdminFinanceOperationsCenter";
import { adminSectionById, type AdminSectionId } from "./AdminSectionRegistry";
import type { Merchant, Order } from "../../types";
import type { FinanceSummary, FinanceSummarySource } from "../../lib/adminData";
import { actionLabel, fieldLabel as translatedFieldLabel, kpiLabel, sectionFallbackLabel, tableColumnLabel } from "../../data/adminTranslations";
import "../../styles/dn-admin-sections.css";

type FinanceArea = "finance_dashboard" | "driver_statements" | "merchant_statements" | "income" | "cod" | "expenses" | "accounts" | "adjustments" | "audit_log";

type Props = {
  id: AdminSectionId;
  isArabic: boolean;
  orders: Order[];
  merchants: Merchant[];
  financeSummary: FinanceSummary | null;
  financeSummarySource?: FinanceSummarySource;
  financeWarning?: string;
  onNavigate?: (id: AdminSectionId) => void;
  onRefresh?: () => Promise<void>;
};

const financeSections = new Set<AdminSectionId>(["finance_dashboard", "driver_statements", "merchant_statements", "income", "cod", "expenses", "accounts", "adjustments", "audit_log"]);
const normalize = (value: unknown) => String(value ?? "").toLowerCase();
const money = (value: unknown, isArabic: boolean) => isArabic ? `${Number(value || 0).toFixed(2)} درهم` : `${Number(value || 0).toFixed(2)} AED`;
const extra = (order: Order) => order as Order & { total?: number; total_amount?: number; service_fee?: number; pickup_city?: string; delivery_city?: string; driver_id?: string; assigned_driver_id?: string };
const tracking = (order: Order) => order.tracking_number || order.invoice_number || order.coupon_number || order.id || "—";
const route = (order: Order) => `${order.sender_city || extra(order).pickup_city || "—"} → ${order.receiver_city || extra(order).delivery_city || order.destination_country || "—"}`;
const amount = (order: Order) => Number(order.cod_amount || order.delivery_price || order.price || extra(order).total || extra(order).total_amount || 0);

const sourceWords: Record<string, string> = { orders: "الطلبات", merchants: "التجار", finance_summary: "ملخص المالية", fallback: "بديل آمن", derived: "مشتق", pricing: "التسعير", local: "محلي", file: "ملف", preview: "معاينة", diagnostics: "تشخيص", support_tickets: "تذاكر الدعم", auth: "المصادقة", ledger_entries: "قيود دفتر الأستاذ", adjustments: "التسويات", audit_log: "سجل التدقيق", summary: "ملخص", settings: "الإعدادات", admin_settings: "إعدادات الإدارة" };
const categoryWords: Record<string, { ar: string; en: string }> = { operations: { ar: "العمليات", en: "Operations" }, orders: { ar: "الطلبات", en: "Orders" }, dispatch: { ar: "التوزيع", en: "Dispatch" }, finance: { ar: "المالية", en: "Finance" }, tools: { ar: "الأدوات", en: "Tools" }, system: { ar: "النظام", en: "System" }, command: { ar: "مركز التحكم", en: "Command" } };
const statusWords: Record<string, { ar: string; en: string }> = { pending: { ar: "قيد الانتظار", en: "Pending" }, cancelled: { ar: "ملغي", en: "Cancelled" }, canceled: { ar: "ملغي", en: "Canceled" }, failed: { ar: "فشل", en: "Failed" }, delivered: { ar: "تم التسليم", en: "Delivered" }, completed: { ar: "مكتمل", en: "Completed" }, returned: { ar: "راجع", en: "Returned" }, postponed: { ar: "مؤجل", en: "Postponed" }, review: { ar: "قيد المراجعة", en: "Under review" }, assigned: { ar: "تم التعيين", en: "Assigned" }, pickup: { ar: "قيد الإحضار", en: "Pickup" } };
function statusText(value: unknown, isArabic: boolean) { const key = normalize(value).replace(/[\s-]+/g, "_"); if (statusWords[key]) return isArabic ? statusWords[key].ar : statusWords[key].en; if (!key) return "—"; return isArabic ? "حالة محفوظة" : key.replace(/_/g, " ").replace(/\b\w/g, (c) => c.toUpperCase()); }
function sourceText(value: string, isArabic: boolean) { if (!isArabic) return value.replace(/[_+]/g, " "); return value.split(/\s*[,+/]\s*|\s+/).filter(Boolean).map((token) => sourceWords[token] || (token.includes("_") ? token.split("_").map((part) => sourceWords[part]).filter(Boolean).join(" ") : "")).filter(Boolean).join(" + ") || "مصدر بيانات"; }
function categoryText(value: string, isArabic: boolean) { const pair = categoryWords[value]; return pair ? (isArabic ? pair.ar : pair.en) : (isArabic ? "وحدة تشغيلية" : value); }
function statusMatch(order: Order, id: AdminSectionId) { const s = normalize(order.status); const text = normalize(`${order.sender_city} ${order.receiver_city} ${extra(order).pickup_city || ""} ${extra(order).delivery_city || ""} ${order.destination_country} ${order.service_type} ${order.shipping_scope} ${order.notes}`); if (["all_orders", "reports", "print"].includes(id)) return true; if (id === "cancelled") return /cancel|fail/.test(s); if (id === "review") return /review|pending|confirm|hold/.test(s); if (id === "postponed") return /postpone|defer|schedule/.test(s); if (id === "returned") return /return/.test(s); if (id === "pickup") return /pick|assign|collect/.test(s); if (id === "abu_dhabi") return /abu dhabi|mussafah|khalifa|mbz|al ain|أبوظبي|ابوظبي/.test(text); if (id === "external") return /international|external|gcc|world|saudi|kuwait|qatar|bahrain|oman|دولي|خارجي/.test(text); if (id === "out_scope") return /out.?of.?scope|unsupported|خارج النطاق/.test(`${text} ${s}`); return true; }
function metricValue(key: string, rows: Order[], merchants: Merchant[], summary: FinanceSummary | null, isArabic: boolean) { const lower = key.toLowerCase(); const delivered = rows.filter((order) => /deliver|complete/.test(normalize(order.status))); const cancelled = rows.filter((order) => /cancel|fail/.test(normalize(order.status))); const cod = rows.reduce((sum, order) => sum + Number(order.cod_amount || 0), 0); const revenue = rows.reduce((sum, order) => sum + Number(order.delivery_price || order.price || extra(order).service_fee || 0), 0); if (lower.includes("merchant") && !lower.includes("payable") && !lower.includes("balance")) return merchants.length; if (lower.includes("delivered")) return delivered.length; if (lower.includes("cancel")) return cancelled.length; if (lower.includes("rate")) return rows.length ? `${Math.round((cancelled.length / rows.length) * 100)}%` : "0%"; if (/cod|balance|payable|revenue|income|expense|net|value|amount|fee|earning/.test(lower)) { if (lower.includes("expense")) return money(summary?.total_expenses || 0, isArabic); if (lower.includes("pending")) return money(summary?.cod_pending || cod, isArabic); if (lower.includes("collected")) return money(summary?.cod_collected || 0, isArabic); if (lower.includes("net")) return money(summary?.net_estimate || revenue, isArabic); return money(revenue || cod, isArabic); } return rows.length; }

export default function AdminSectionWorkspace({ id, isArabic, orders, merchants, financeSummary, financeSummarySource = "derived", financeWarning, onNavigate, onRefresh }: Props) {
  const config = adminSectionById[id]; const [query, setQuery] = useState(""); const [filters, setFilters] = useState<Record<string, string>>({}); const [notice, setNotice] = useState(""); const navigate = (target: AdminSectionId) => onNavigate?.(target); const refresh = onRefresh || (async () => undefined); const isFinance = financeSections.has(id);
  const baseRows = useMemo(() => orders.filter((order) => statusMatch(order, id)), [id, orders]);
  const rows = useMemo(() => baseRows.filter((order) => { const haystack = [tracking(order), order.receiver_phone, order.sender_phone, order.receiver_name, order.customer_name, order.merchant_name, order.sender_name, order.status, order.service_type, order.payment_method, order.sender_city, order.receiver_city].join(" ").toLowerCase(); if (query && !haystack.includes(query.toLowerCase())) return false; if (filters.status && !normalize(order.status).includes(normalize(filters.status))) return false; if (filters.merchant && !normalize(`${order.merchant_id || ""} ${order.merchant_name || ""} ${order.sender_name || ""}`).includes(normalize(filters.merchant))) return false; if (filters.driver && !normalize(`${order.driver_code || ""} ${order.driver_name || ""} ${order.driver_phone || ""} ${extra(order).driver_id || ""} ${extra(order).assigned_driver_id || ""}`).includes(normalize(filters.driver))) return false; if ((filters.emirate || filters.city) && !normalize(`${order.sender_city} ${order.receiver_city}`).includes(normalize(filters.emirate || filters.city))) return false; if (filters.codOnly && Number(order.cod_amount || 0) <= 0) return false; return true; }).slice(0, 120), [baseRows, filters, query]);
  const title = isArabic ? config.titleAr : config.titleEn; const subtitle = isArabic ? config.subtitleAr : config.subtitleEn; const source = sourceText(config.dataSource, isArabic); const totals = { orders: baseRows.length, visible: rows.length, cod: money(baseRows.reduce((sum, order) => sum + Number(order.cod_amount || 0), 0), isArabic), income: money(baseRows.reduce((sum, order) => sum + Number(order.delivery_price || order.price || 0), 0), isArabic) };
  const doAction = (action: string) => { if (action === "addOrder") navigate("new_order"); else if (action === "addMerchant") navigate("new_merchant"); else if (action === "reviewPending") navigate("review"); else if (action === "openFinance") navigate("finance_dashboard"); else if (action === "openIncome") navigate("income"); else if (action === "openExpenses") navigate("expenses"); else if (action === "openCod") navigate("cod"); else if (action === "openStatements") navigate("merchant_statements"); else setNotice(isArabic ? `تم فتح إجراء: ${actionLabel(action, true)}` : `Opened action: ${actionLabel(action, false)}`); };
  if (isFinance) return <AdminFinanceOperationsCenter isArabic={isArabic} activeSection={id as FinanceArea} orders={orders} merchants={merchants} financeSummary={financeSummary} financeSummarySource={financeSummarySource} onRefresh={refresh} onNavigate={(target) => navigate(target as AdminSectionId)} />;
  return <section className="dn-section-workspace" dir={isArabic ? "rtl" : "ltr"}><header className="dn-section-hero"><div><span>{categoryText(config.category, isArabic)} · {source}</span><h1>{title}</h1><p>{subtitle}</p></div><div className="dn-section-hero-actions"><button type="button" onClick={() => void refresh()}>{isArabic ? "تحديث" : "Refresh"}</button><AdminPdfExportButton payload={{ language: isArabic ? "ar" : "en", sectionTitle: title, filters: Object.entries(filters).map(([key, value]) => `${translatedFieldLabel(key, isArabic)}: ${value || (isArabic ? "الكل" : "All")}`).join(" | ") || (isArabic ? "بدون فلاتر" : "No filters"), totals, columns: [{ key: "tracking", label: isArabic ? "التتبع" : "Tracking" }, { key: "status", label: isArabic ? "الحالة" : "Status" }, { key: "merchant", label: isArabic ? "التاجر" : "Merchant" }, { key: "route", label: isArabic ? "المسار" : "Route" }, { key: "amount", label: isArabic ? "المبلغ" : "Amount" }], rows: rows.map((order) => ({ tracking: tracking(order), status: statusText(order.status || "", isArabic), merchant: order.merchant_name || order.sender_name || "—", route: route(order), amount: money(amount(order), isArabic) })) }} /></div></header>{id === "dashboard" && <AdminLiveOperationsMap isArabic={isArabic} orders={orders} />}{financeWarning && <p className="dn-clean-note">{isArabic ? "ملخص مالي مشتق مؤقتاً من الطلبات" : "Finance summary temporarily derived from orders"}</p>}<div className="dn-section-kpis">{config.kpis.slice(0, 8).map((key) => <article key={key}><span>{kpiLabel(key, isArabic)}</span><strong>{metricValue(key, baseRows.length ? baseRows : orders, merchants, financeSummary, isArabic)}</strong><small>{source}</small></article>)}</div><div className="dn-section-panels"><article><h3><Filter />{isArabic ? "الفلاتر والمدخلات" : "Filters & inputs"}</h3><div className="dn-section-form"><label><span>{isArabic ? "بحث" : "Search"}</span><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder={isArabic ? "تتبع، هاتف، تاجر..." : "Tracking, phone, merchant..."} /></label>{[...config.filters, ...config.inputFields].slice(0, 12).map((field) => <label key={field}><span>{translatedFieldLabel(field, isArabic)}</span><input value={filters[field] || ""} onChange={(event) => setFilters((prev) => ({ ...prev, [field]: event.target.value }))} placeholder={translatedFieldLabel(field, isArabic)} /></label>)}</div></article><article><h3><Sparkles />{isArabic ? "إجراءات جاهزة" : "Ready actions"}</h3><div className="dn-action-grid">{config.actions.map((action) => <button key={action} type="button" onClick={() => doAction(action)}>{actionLabel(action, isArabic)}</button>)}</div><p className="dn-clean-note">{isArabic ? "كل إجراء يفتح القسم المناسب أو يعرض رسالة آمنة دون أخطاء خام." : "Each action opens the right section or shows a safe message without raw errors."}</p></article></div><article className="dn-section-table-card"><h3><FileText />{isArabic ? "الصفوف الحالية" : "Current rows"}</h3>{notice && <p className="dn-clean-note">{notice}</p>}<div className="dn-section-table-wrap"><table><thead><tr><th>{tableColumnLabel("tracking", isArabic)}</th><th>{tableColumnLabel("status", isArabic)}</th><th>{tableColumnLabel("merchantSender", isArabic)}</th><th>{tableColumnLabel("route", isArabic)}</th><th>{tableColumnLabel("receiver", isArabic)}</th><th>{tableColumnLabel("amount", isArabic)}</th></tr></thead><tbody>{rows.map((order) => <tr key={String(order.id || tracking(order))}><td>{tracking(order)}</td><td>{statusText(order.status || "", isArabic)}</td><td>{order.merchant_name || order.sender_name || "—"}</td><td>{route(order)}</td><td>{order.receiver_name || order.customer_name || "—"}</td><td>{money(amount(order), isArabic)}</td></tr>)}</tbody></table>{!rows.length && <div className="dn-empty-state"><Search className="h-5 w-5" />{sectionFallbackLabel("noMatchingOrders", isArabic)}</div>}</div></article></section>;
}
