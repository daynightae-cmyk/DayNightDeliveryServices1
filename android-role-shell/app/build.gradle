plugins {
    id "com.android.application"
}

def releaseKeystorePath = System.getenv("DAYNIGHT_ANDROID_KEYSTORE")

android {
    namespace "com.daynightae.shell"
    compileSdk 35

    defaultConfig {
        minSdk 24
        targetSdk 35
        versionCode 1
        versionName "1.0.0"
    }

    signingConfigs {
        release {
            if (releaseKeystorePath) {
                storeFile file(releaseKeystorePath)
                storePassword System.getenv("DAYNIGHT_ANDROID_STORE_PASSWORD")
                keyAlias System.getenv("DAYNIGHT_ANDROID_KEY_ALIAS")
                keyPassword System.getenv("DAYNIGHT_ANDROID_KEY_PASSWORD")
            }
        }
    }

    flavorDimensions += "role"
    productFlavors {
        driver {
            dimension "role"
            applicationId "com.daynightae.driver"
            resValue "string", "app_name", "DAY NIGHT Driver"
            resValue "string", "app_name_ar", "داي نايت للمندوب"
            buildConfigField "String", "ROLE", '"driver"'
            buildConfigField "String", "START_URL", '"https://www.daynightae.com/driver?nativeShell=driver&nosplash=1"'
        }
        merchant {
            dimension "role"
            applicationId "com.daynightae.merchant"
            resValue "string", "app_name", "DAY NIGHT Merchant"
            resValue "string", "app_name_ar", "داي نايت للتاجر"
            buildConfigField "String", "ROLE", '"merchant"'
            buildConfigField "String", "START_URL", '"https://www.daynightae.com/merchant?nativeShell=merchant&nosplash=1"'
        }
    }

    buildTypes {
        debug {
            debuggable true
        }
        release {
            minifyEnabled false
            shrinkResources false
            if (releaseKeystorePath) {
                signingConfig signingConfigs.release
            }
            proguardFiles getDefaultProguardFile("proguard-android-optimize.txt"), "proguard-rules.pro"
        }
    }

    buildFeatures {
        buildConfig true
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }

    lint {
        abortOnError true
        checkReleaseBuilds true
    }

    packaging {
        resources {
            excludes += ["META-INF/DEPENDENCIES", "META-INF/LICENSE*", "META-INF/NOTICE*"]
        }
    }
}
